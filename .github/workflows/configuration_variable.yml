name:
  variable
on:
  push
env:
  cloud: aws
jobs:
  getting_job:
    runs-on: ubuntu-latest
    env:
      gretting: hello
    steps:
      - name: "HI"
        run: echo "hello $First_Name $gretting $cloud ${{ vars.PROJECT_ID }}"
        env:
          First_Name: Jetha
