name: Self hosted runner

on: [push]

jobs: 
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name : build command
        run : |
          echo add self-hosted runner
    
